"""Multi-step operations composing repositories (series generation, later:
daily summary, JSON export). No Qt imports here — the P2 voice and agent-API
features must be able to call these headlessly.
"""
